[package]
name = "secure-http"
version = "0.1.0"
edition = "2021"
description = "Native TLS HTTP client for providers that reject rustls fingerprints"
license = "MIT"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "native-tls"] }